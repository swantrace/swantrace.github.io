export const SectionTitle = ({ children }: { children: any }) => (
  <h2 className="my-8 text-base uppercase tracking-[0.25em] text-windsurfing dark:text-blue-dark">
    {children}
  </h2>
);

export default SectionTitle;
